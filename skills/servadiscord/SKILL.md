---
name: servadiscord
description: Manage a ServaDiscord workspace graph through MCP tools.
---

Start with `list_workspaces`, then `list_nodes`. Call `describe_node_type` before adding or changing node data. Use the returned node schema exactly. Never request or store a bot token.
